<h2>shopping cart</h2>
<% if @books_in_cart.empty?%>
  什麼都沒有...購物車是空的!
<% else %>
  <ul>
    <% @books_in_cart.each do |book| %>
      <li>
        書名 <%= book[:book][:title]%>
        $$ <%= book[:book][:price]%>
        數量 <%= book[:quantity]%>
        小計 <%= book[:sub_total]%>
      <% end %>
    </li>
    <li>總額: <%= @total_price%></li>
  </ul>
  <%= link_to '清空購物車', cart_path, data: { turbo_confirm: '確定要清空購物車嗎？', turbo_method: :delete }, class: 'btn btn-danger' %>
<% end %>
